# PL Export Inventory

Current exported artefacts for the used FPGA build:

- `../export/design_1_wrapper.bit`
  FPGA bitstream used to program the board.
- `../export/design_1_wrapper.hwh`
  hardware handoff metadata used by PYNQ to discover the design and register
  map.

Add future artefacts here as needed, for example:

- `.xsa`
- block-design screenshots
- timing/utilization reports
- short notes describing which export was used in the final demo
